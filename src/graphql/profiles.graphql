query findProfiles($first: Int!, $name: String!) {
  profilesCollection(first: $first, filter: { nickname: { eq: $name } }) {
    edges {
      node {
        id
        nickname
        avatar_url
      }
    }
  }
}
